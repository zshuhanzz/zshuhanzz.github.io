/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/website_2",
  assetPrefix: "/website_2/",
  images: { unoptimized: true },
};

export default nextConfig;
